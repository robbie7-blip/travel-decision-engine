// Single source of truth for the two facts /terms, /privacy, /cookies, and
// SiteFooter.tsx all need but nothing in this codebase (or this project's
// setup so far) actually establishes: who legally operates decide, and a
// real monitored inbox for support/legal requests. Deliberately left as
// obvious placeholders rather than invented values — a fabricated company
// name or a support address nobody reads is worse than a visible TODO.
// Replace both before this governs real, paying customers.

export const OPERATOR_NAME = "[operator name — replace before launch]";
export const CONTACT_EMAIL = "[support email — replace before launch]";
